{
  "name": "holster-desktop-ui",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Holster desktop UI (SvelteKit, served by Tauri)",
  "scripts": {
    "dev": "npx vite dev --port 1420 --strictPort",
    "build": "npx vite build",
    "preview": "npx vite preview --port 1420 --strictPort",
    "check": "npx svelte-check --tsconfig ./tsconfig.json",
    "tauri": "npx tauri"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tauri-apps/cli": "^2",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tslib": "^2.7.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-clipboard-manager": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  }
}
