//! Vault data models.
//!
//! T1.1 stubs. Real Provider enum, KeyStatus enum, and KeyMetadata struct
//! are authored in T1.4 — see docs/framework/06_MILESTONE_1_TASKS.md.

#[derive(Debug)]
pub struct Provider;

#[derive(Debug)]
pub struct KeyStatus;

#[derive(Debug)]
pub struct KeyMetadata;
